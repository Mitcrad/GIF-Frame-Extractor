# GIF-Frame-Extractor
This tool significantly reduces the size of a GIF by lowering the number of animation frames, without affecting the image quality. It merges the duration of the skipped frames into the previous kept frame, so **the total animation length and playback speed remain the same**, but the motion may look a bit less smooth.
